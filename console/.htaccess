# Keep directory listing off
Options -Indexes

# Minimal network boundary: allow only specific IP ranges (replace with yours)
<RequireAll>
  Require ip 203.0.113.0/24
  Require ip 2001:db8::/32
</RequireAll>

# Note: App-layer should still enforce X-Api-Key and request validation

