# Sample Database Package

This directory contains a standalone copy of the `database/sample` tree for local testing. To use it:

1. Copy the contents of `sample/` into the project root.
2. Run `./console/test_sample.sh` to spin up the sample endpoints.

This keeps sample assets out of the production `database/` directory so that the UI stays clean.
